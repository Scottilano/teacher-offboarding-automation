# Examples only; .env is not automatically loaded. Export in your shell if needed.
# OFFBOARDING_NODE=/path/to/node
# OFFBOARDING_PYTHON=/path/to/python
# PLAYWRIGHT_BROWSERS_PATH=/path/to/playwright-browsers
# Optional legacy Google Sheets CLI only (local app does not use Google):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/google-service-account.json
